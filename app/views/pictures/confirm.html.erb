<h3 class="page-title">投稿確認画面</h3>

<%= form_with(model: @picture, url: pictures_path, local: true) do |form| %>
  <div class="form-area">
    <%= form.hidden_field :image_cache %>
    <%= form.hidden_field :title %>
    <%= form.hidden_field :content %>
    
    <div class="text-center">
      <p><%= image_tag @picture.image.url(:middle) %></p>
      <p>タイトル:<%= @picture.title %></p>
      <p>本文:<%= @picture.content %></p>
      <%= form.submit  "登録する", class: "btn btn-success" %>
    
  </div>
<% end %>

<%= form_with(model: @picture, url: new_picture_path, local: true, method: 'get') do |form| %>
  <%= form.hidden_field :title %>
  <%= form.hidden_field :content %>

  <%= form.submit "もどる", name: 'back', class: "btn btn-default "%>
<% end %>

</div>
